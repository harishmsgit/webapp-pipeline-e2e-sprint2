FROM hashicorp/terraform:1.6

# Install AWS CLI v1 and other dependencies for production-grade pipeline execution.
RUN apk add --no-cache \
    python3 \
    py3-pip \
    docker-cli \
    git \
    bash \
  && pip3 install --no-cache-dir --break-system-packages awscli

ENTRYPOINT []